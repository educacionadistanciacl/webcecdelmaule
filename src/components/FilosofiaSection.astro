---
import IconItem from './IconItem.astro';
const path_icon = '../public/img/icons';

const iconItems = [
  { icon: `${path_icon}/icon-graph-up.svg`, label: "Crecimiento profesional", percentage: "70", colorIcon: "#FDC115" },
  { icon: `${path_icon}/icon-sun-ligth.svg`, label: "Nuevas oportunidades", percentage: "93", colorIcon: "#F46822" },
  { icon: `${path_icon}/icon-tools.svg`, label: "Aumento en la productividad", percentage: "82", colorIcon: "#DB1A5D" },
  { icon: `${path_icon}/icon-verified-user.svg`, label: "Satisfacción de nuestros alumnos", percentage: "88", colorIcon: "#643090" }
];
---

<div class="filosofia-container">
  <h3 class="section-title">Nuestra Filosofia</h3>
  <p class="section-description">Creemos en el crecimiento profesional de todas las personas, generando un impacto positivo en sus puestos de trabajo.</p>
  <div class="filosofia-section">
    {iconItems.map(item => (
      <IconItem
        icon={item.icon}
        label={item.label}
        percentage={item.percentage}
        colorIcon={item.colorIcon}
        additionalClass="hover"
      />
    ))}
  </div>
</div>

<style>
.filosofia-container {
  display: flex;
  flex-direction: column; /* Apila los elementos verticalmente */
  width: 60%; /* Usa solo el 60% del ancho disponible */
  margin: auto; /* Centra el contenedor en la página con márgenes automáticos */
  text-align: center;
  padding: 20px; /* Padding vertical ajustado */
  min-height: 550px; /* Altura general del contenedor */
  justify-content: center; /* Centra los elementos verticalmente */
  align-items: center; /* Centra los elementos horizontalmente */
}

.section-title {
  color: var(--color-primario);
  font-size: 24px;
  margin-top: 20px; /* Espacio adicional arriba del título */
  margin-bottom: 10px; /* Menos espacio antes de la descripción */
}

.section-title h3 {
  text-transform: uppercase;
}

.section-description {
  color: var(--color-primario);
  font-size: 16px;
  margin-bottom: 40px; /* Aumenta el espacio antes de la sección de íconos */
  padding: 0 20%; /* Añade padding a los lados para más enfoque en el texto */
}

.filosofia-section {
  display: flex;
  justify-content: space-evenly; /* Asegura un espaciado uniforme entre los ítems */
  align-items: center;
  flex-wrap: wrap; /* Evita que los elementos se envuelvan */
  gap: 15px; /* Ajusta el espacio entre los ítems si es necesario */
  padding: 0 5%; /* Reduce el padding para que los ítems estén más juntos */
}

</style>