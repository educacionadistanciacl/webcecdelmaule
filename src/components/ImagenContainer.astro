---
const {ImagenBackgounrd} = Astro.props;
---

<div class="imagenContainer" style={{ backgroundImage: `url(${ImagenBackgounrd})` }}> 
    <slot/>
</div>

<style>
.imagenContainer {
  display: flex;
  flex-direction: column;
  justify-content: center;
  align-items: center;
  height: calc(100vh - 100px);  /* Ajusta según la altura del header */
  /* background-image: url('/public/img/hero.svg'); */
  background-size: cover;
  background-position: center;
  background-repeat: no-repeat;
  color: var(--color-secundario);
  position: relative;  /* Permite posicionamiento absoluto de los hijos */
}
</style>